{
  "name": "zbyte-keycloak-verify",
  "version": "1.0.0",
  "description": "Verify zbyte keycloak service. Checks if the token is valid and retrived claims from the token.",
  "main": "index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepare": "npm run build",
    "prepublishOnly": "npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Zbyteio/zbyte-keycloak-verify.git"
  },
  "keywords": [],
  "author": "Milind Sahoo",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Zbyteio/zbyte-keycloak-verify/issues"
  },
  "homepage": "https://github.com/Zbyteio/zbyte-keycloak-verify#readme",
  "devDependencies": {
    "@types/node": "^18.14.1",
    "prettier": "^2.8.4",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@types/ramda": "^0.28.23",
    "axios": "^1.3.4",
    "jsonwebtoken": "^9.0.0",
    "jwk-to-pem": "^2.0.5"
  },
  "files": ["lib/**/*"]
}
